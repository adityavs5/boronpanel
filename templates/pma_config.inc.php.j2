<?php
// Boron-managed file (Phase 3 feature 3). Regenerated by
// daemon/pma.py's bootstrap_pma() -- manual edits here will be
// overwritten. Signon auth only: there is no interactive username/
// password login for this phpMyAdmin install, only tokens minted by
// Boron's own "Manage" button (POST /accounts/{u}/databases/{db}/
// pma-token), each scoped to exactly one database via a short-lived
// ephemeral MariaDB user.

$cfg['blowfish_secret'] = {{ blowfish_secret|tojson }};

$i = 0;
$i++;
$cfg['Servers'][$i]['auth_type'] = 'signon';
$cfg['Servers'][$i]['SignonSession'] = 'PMASignon';
$cfg['Servers'][$i]['SignonURL'] = '/boron_signon.php';
$cfg['Servers'][$i]['host'] = 'localhost';
$cfg['Servers'][$i]['socket'] = {{ mariadb_socket|tojson }};
$cfg['Servers'][$i]['compress'] = false;
$cfg['Servers'][$i]['AllowNoPassword'] = false;

$cfg['TempDir'] = '/var/lib/phpmyadmin/tmp';
$cfg['LoginCookieValidity'] = {{ token_ttl_seconds }};
