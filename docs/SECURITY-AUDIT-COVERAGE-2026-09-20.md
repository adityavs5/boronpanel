# Security audit coverage checkpoint — 2026-09-20

This is a working ledger, not an audit completion or certification claim.
The machine-readable inventory is `SECURITY-AUDIT-COVERAGE-2026-09-20.csv`,
generated by `scripts/security_inventory.py` with disposable configuration.
It currently lists 430 HTTP/WebSocket route-method entries, 414 root RPC
operations, 14 cron entries, three static services, three generated account
services, seven shell scripts, 25 Python scripts, and one native helper: 897 total. Ten
deliberately public routes are marked `reviewed-public` and locked by
`tests/test_public_route_inventory.py`; the other 887 entries remain
pending until their authorization, ownership, side effects, and negative tests
have been individually reviewed.
In particular, a declared FastAPI dependency is not evidence that the
underlying root handler enforces account ownership.

## Boundaries examined so far

| Boundary | Current evidence | Disposition |
| --- | --- | --- |
| Browser to terminal WebSocket | Foreign and missing Origin rejected before cookie authentication; direct negative tests | Source fixed, deployment pending |
| Browser to FileBrowser proxy | Cookie-authenticated writes require same Origin; direct negative tests | Live one-file hotfix; browser regression pending |
| FileBrowser-served active documents | Dynamic inline-script hashing now applies only to its exact SPA shell URL with no query; other HTML is sandboxed and attached, while SVG/XML is sandboxed; five focused tests and live API health passed | Live one-file hotfix; real browser/file delivery check pending |
| Uploaded SVG to panel browser | XML drawing allowlist; external names, DTDs, processing instructions, event handlers and references rejected; valid logo round trip | Source fixed, deployment pending |
| Customer database user to other databases | Legacy grant wildcard reproduced against temporary MariaDB databases; 10 existing broad grants repaired and verified | Live repaired; source/upgrade code pending deployment |
| Imported cPanel and backup SQL to MariaDB | `mysql --binary-mode` blocks client-side shell commands; exclusive `--defaults-file` preserves scoped credentials; backup restore gets a one-use target-DB user. Disposable live tests proved normal writes and denied cross-database writes, including after the three-module hotfix | Live hotfix verified; signed release and full restore/import workflows pending |
| Application and portable archives | Download/member/path/expansion limits; Drupal tar validates regular files and directories before extraction; nested portable tar validation bounded | App-installer module hotfixed; portable archive source-only; full-system large-archive tests pending |
| WordPress release ZIP | Download and extraction byte/member/path limits, duplicate normalized paths rejected before writing; 32 focused tests | Installed WordPress module hotfixed and panel healthy; real install and provenance tests pending |
| WordPress installer credential output | `wp_install()` returns the plaintext password; helper output now contains only a success boolean and daemon errors omit raw stdout/stderr. 39 focused tests and live health passed; six existing job rows had no failed-job exposure | Live two-file hotfix; real install in disposable lab pending |
| WP-CLI password reset | New password moved from `--user_pass=` argv to documented stdin prompt; command runner masks known secrets in persisted output/errors. 21 focused tests and live health passed; 30 existing command jobs showed no prior reset job | Live two-file hotfix; real reset and cross-account procfs check pending |
| MaxMind GeoLite download | Optional license key moved from curl argv to stdin config; compressed/member/expanded database limits and missing-archive guard; three focused tests | Live one-file hotfix; no MaxMind key configured, authenticated download pending |
| PrestaShop CLI installer | Database/admin passwords passed to a root-owned PHP helper over stdin and reconstructed as PHP `$argv`; 21 focused app-installer tests and a live unprivileged synthetic helper run passed | Live two-file hotfix; real vendor install in disposable lab pending |
| Application install worker errors | Arbitrary vendor/DB exception text no longer reaches job rows or service logs; a secret-bearing failure test passes | Live one-file hotfix; failed vendor workflow pending |
| Root RPC error responses and audit | Unexpected handler text and parser errors suppressed; validation messages redact known secret inputs; 10 focused and 58 adjacent tests, live read-only RPC and health pass | Live one-file hotfix; typed user-facing error review pending |
| Host listeners and scheduling | Protected live snapshot recorded 59 TCP/UDP listener rows, 208 service rows and 25 timer rows; MariaDB, Redis, PowerDNS API, FileBrowser and SpamAssassin bind loopback; panel 2222 and OLS WebAdmin 7080 bind publicly by design | Listener/firewall policy and cron/dynamic-service authority review pending |
| LiteSpeed repository trust | Bundled public key verifies the cached signed Noble Release. Live host migrated from historical HTTP/global keys to HTTPS `signed-by`; normal APT refresh passed with unchanged OpenLiteSpeed candidate. Staged updater reconciliation backs up and rolls back on failure | Live verified; fresh installer/updater drill in VM pending |
| External IMAP to root migration worker | Validated public IP is passed directly to imapsync; original hostname used for SNI/certificate verification; plaintext disabled; 21 focused tests | Source fixed; controlled TLS integration pending |
| GitHub asset to root updater | Ed25519 publisher signature bound to version and digest; extraction verifies the exact open file handle again; missing/wrong/swapped archives rejected in unit tests | Source fixed; trust bootstrap and artifact acceptance pending |
| Global spam Sieve write to mail service | Config transaction restores previous script after compile/reload failure; regression test | Source fixed, deployment pending |
| Plan count limits | Database, subdomain, FTP, mailbox and combined Node/Python app creation reject at-cap accounts in root handlers; focused negative test | Source fixed, deployment pending; bandwidth remains alert-only |
| Historical Redis socket squatting (A2-5) | Current source places each socket in the account's private `~/.redis`; installed config uses that path and socket/parent modes are 0700; no legacy shared sockets found | Historical finding no longer applies to current implementation |
| OpenLiteSpeed WebAdmin TLS | Local port 7080 handshake negotiated TLS 1.3 and verified the panel hostname against its trusted certificate | Live verified |
| Ubuntu package maintenance | Refreshed apt indexes show 324 upgradable packages, 170 with a security-pocket candidate; simulation would upgrade 317 and leave 7 back | Patch assessment and controlled maintenance pending |
| Panel certificate to FTPS | Live FTP certificate replaced with trusted panel certificate, TLS 1.3/hostname verification passed; renewal/update hooks added | Live verified; source deployment pending |
| API UID to root daemon | Root-owned session/token resolution and explicit policies now cover 414 operations; forged metadata rejected on the disposable VM. Every operation has an anonymous-access negative check; all admin policy groups reject customers/resellers, and all shared account policy groups reject foreign/mismatched targets | Implemented and VM-tested; migration, rollback and full resource review remain release gates |

## Entry-point classification still required

The route inventory found 405 route-method entries with direct
`get_identity` dependencies, 14 FileBrowser proxy methods and one WebSocket
with manual authentication, and 10 deliberately public entries (login,
health, branding reads and SPA/root). The exact public set is locked in CI.
These are dependency observations, not completed role/resource decisions.
Cron, static/dynamic service, shell, Python and native-helper rows now exist; their
individual authority and side effects still need review. Asynchronous job
continuations and actual listener policy also need dedicated dispositions.

The audit plan uses OWASP ASVS 5.0.0 Level 2 as the baseline and selected
Level 3 requirements for administration, isolation, authentication, secrets
and updates. A requirement-by-requirement ASVS/WSTG mapping is pending. No
claim of conformance should be made from this checkpoint.

## Verification limits

The sandbox cannot run systemd, network namespaces or real service sockets.
Unit tests use a disposable configuration. Bounded read-only host checks and
the specifically recorded FTPS/database-grant remediations were executed
outside that sandbox. A disposable full Ubuntu/OLS/MariaDB/mail/systemd lab,
fresh-install/upgrade/rollback drills, and browser security checks are still
required for the plan's completion standard. The online npm advisory request
was rejected by automatic approval review; the offline npm audit found no
cached advisories and cannot stand in for a current advisory check.

The broader existing handler suite includes tests that write service files
under `/etc/systemd/system` and certificate paths. Those fail under this
sandbox's read-only host mounts; they require the isolated full-system lab.

The CSV now records each RPC policy name from `daemon/rpc_policy.py`. This is policy coverage, not a completed review of each handler. Pending dispositions are retained. The Ubuntu VM passed all 120 cPanel/portable archive, WordPress, app installer, and update-finalizer tests at source revision `0d0522b`; this includes the two earlier host-dependent cPanel cases. The previously stalled malware, snapshot, and update API suites passed 25 tests outside the restricted local sandbox.
