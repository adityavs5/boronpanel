# Boron-managed ACME renewal. Certbot and its DNS plugins live in Boron's
# virtualenv, so do not call a distribution certbot binary here.
17 3,15 * * * root /opt/boron/.venv/bin/certbot renew --quiet >> /var/log/boron/certbot-renew.log 2>&1
