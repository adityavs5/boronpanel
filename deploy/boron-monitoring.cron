# Boron service health monitoring (Run A feature 5).
# Install to /etc/cron.d/boron-monitoring (root:root, 0644):
#   install -m 0644 deploy/boron-monitoring.cron /etc/cron.d/boron-monitoring
#
# Every 5 minutes: systemctl is-active per monitored service (OLS, Postfix,
# Dovecot, PowerDNS, MariaDB, Pure-FTPd, fail2ban), history row each, admin
# alert email on down + recovery (30 min re-alert cooldown per service).
*/5 * * * * root /opt/boron/scripts/monitoring_check.py >> /var/log/boron/monitoring-check.log 2>&1
